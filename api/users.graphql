enum Role {
    member
    admin
}

type User {
    id: ID!
    role: Role!
    email: String!
    login: String!
    createdAt: DateTime!
    updatedAt: DateTime!
}

input UpdateUserInput {
    email: String
    login: String
}

extend type Mutation {
    updateUser(input: UpdateUserInput!): User!
}
